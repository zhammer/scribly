root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/main ./cmd/site/"
bin = "tmp/main"
include_ext = ["go", "tpl", "tmpl", "css"]
# Ignore these filename extensions or directories.
exclude_dir = ["cypress", "tmp", "node_modules"]
